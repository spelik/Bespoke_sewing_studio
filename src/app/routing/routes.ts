import type { Page } from "../types";

export const PAGE_PATHS: Record<Page, string> = {
  home: "/",
  services: "/services",
  portfolio: "/portfolio",
  order: "/order",
  about: "/about",
  contact: "/contact",
  privacy: "/privacy",
  admin: "/admin",
};
